# LEARNINGS

## General

1. npm create is an alias of npm init
2. npm init is used to initialise a project and set up a new or existing npm package. npm install will only install a given package in node_modules and will generate a bare bones package.json + lock file without default scripts and only the specified package
